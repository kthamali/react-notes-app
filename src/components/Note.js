const Note = () => {
    return (
        <div className="note">
            <span>First Note!!</span>
            <div className="note-footer">
            </div>

        </div>

    );

};

export default Note;